# Put in /etc/systemd/journald.conf.d/ and run: systemctl restart systemd-journald
# The server's log goes to the journal. Cap it so a noisy day can never fill the disk.
[Journal]
SystemMaxUse=500M
SystemKeepFree=1G
MaxRetentionSec=7day
