{
  "name": "stockastic",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w @stockastic/web",
    "build": "npm run build -w @stockastic/config && npm run build -w @stockastic/web"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
