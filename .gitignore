node_modules/
*.log
.env
.env.local

# Go
apps/api/bin/
*.exe
coverage.out

# The frontend build is embedded into the Go binary. Only the placeholder is tracked, so `go build`
# works on a fresh clone before the frontend has ever been built.
apps/api/internal/webui/dist/*
!apps/api/internal/webui/dist/.gitkeep

# The organisers' rulebook is kept out of the repository
Live_Financial_Ecosystem_Rulebook_v1.1.pdf

# Runtime data (the durable log) and local secrets
apps/api/data/
*.wal
